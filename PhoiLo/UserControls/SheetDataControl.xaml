<UserControl x:Class="PhoiLo.UserControls.SheetDataControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Loaded="UserControl_Loaded">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="KẾT NỐI GOOGLE SHEET" FontWeight="Bold" FontSize="20" Foreground="#2c3e50" Margin="0,0,0,15" Grid.Row="0"/>
        
        <Grid Grid.Row="1" Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="120"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock Text="Client ID:" Grid.Row="0" Grid.Column="0" VerticalAlignment="Center" Margin="0,5" FontWeight="SemiBold"/>
            <TextBox x:Name="TxtClientId" Grid.Row="0" Grid.Column="1" Margin="0,5" Height="28" Padding="5,2"/>

            <TextBlock Text="Client Secret:" Grid.Row="1" Grid.Column="0" VerticalAlignment="Center" Margin="0,5" FontWeight="SemiBold"/>
            <TextBox x:Name="TxtClientSecret" Grid.Row="1" Grid.Column="1" Margin="0,5" Height="28" Padding="5,2"/>

            <TextBlock Text="Sheet ID:" Grid.Row="2" Grid.Column="0" VerticalAlignment="Center" Margin="0,5" FontWeight="SemiBold"/>
            <TextBox x:Name="TxtSheetId" Grid.Row="2" Grid.Column="1" Margin="0,5" Height="28" Padding="5,2"/>
            
            <TextBlock Text="Vùng lấy dữ liệu:" Grid.Row="3" Grid.Column="0" VerticalAlignment="Center" Margin="0,5" FontWeight="SemiBold"/>
            <TextBox x:Name="TxtRange" Grid.Row="3" Grid.Column="1" Margin="0,5" Height="28" Text="Sheet1!A1:Z100" Padding="5,2"/>

            <Button x:Name="BtnConnect" Content="🚀 KẾT NỐI &amp; LẤY DỮ LIỆU" Grid.Row="4" Grid.Column="1" Height="40" Margin="0,15,0,0" Click="BtnConnect_Click" HorizontalAlignment="Left" Width="200" Background="#3498db" Foreground="White" FontWeight="Bold" BorderThickness="0" Cursor="Hand"/>
        </Grid>

        <Border Grid.Row="2" BorderBrush="#bdc3c7" BorderThickness="1" CornerRadius="5" Background="#f8f9fa">
            <Grid>
                <TextBlock x:Name="TxtStatus" Text="😴 Chưa có dữ liệu. Hãy nhập thông tin và kết nối..." 
                           HorizontalAlignment="Center" VerticalAlignment="Center" 
                           FontSize="16" Foreground="#7f8c8d" FontStyle="Italic"/>
                
                <DataGrid x:Name="SheetDataGrid" Visibility="Collapsed" 
                          AutoGenerateColumns="True" IsReadOnly="True" 
                          AlternatingRowBackground="#ecf0f1" RowBackground="White"
                          HeadersVisibility="Column" GridLinesVisibility="Horizontal" 
                          HorizontalGridLinesBrush="#e0e0e0" BorderThickness="0">
                    <DataGrid.ColumnHeaderStyle>
                        <Style TargetType="DataGridColumnHeader">
                            <Setter Property="Background" Value="#2c3e50"/>
                            <Setter Property="Foreground" Value="White"/>
                            <Setter Property="FontWeight" Value="Bold"/>
                            <Setter Property="Padding" Value="10,8"/>
                        </Style>
                    </DataGrid.ColumnHeaderStyle>
                    <DataGrid.CellStyle>
                        <Style TargetType="DataGridCell">
                            <Setter Property="Padding" Value="10,5"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="DataGridCell">
                                        <Border Padding="{TemplateBinding Padding}" Background="{TemplateBinding Background}">
                                            <ContentPresenter VerticalAlignment="Center"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </DataGrid.CellStyle>
                </DataGrid>
            </Grid>
        </Border>
    </Grid>
</UserControl>